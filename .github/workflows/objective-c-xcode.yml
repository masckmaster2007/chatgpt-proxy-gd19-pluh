name: Build GDPS Proxy Dylib

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Download iOS 11.4 SDK
      run: |
        curl -L -o iPhoneOS11.4.sdk.zip https://github.com/xybp888/iOS-SDKs/releases/download/iOS-SDKs/iPhoneOS11.4.sdk.zip
        unzip iPhoneOS11.4.sdk.zip
        sudo mv iPhoneOS11.4.sdk /tmp/iPhoneOS11.4.sdk
    
    - name: Build armv7 (32-bit)
      run: |
        clang \
          -arch armv7 \
          -miphoneos-version-min=7.0 \
          -dynamiclib \
          -o gdps_proxy_armv7.dylib \
          gdps_proxy.c \
          -framework Security \
          -framework CoreFoundation \
          -framework CFNetwork \
          -isysroot /tmp/iPhoneOS11.4.sdk \
          -F/tmp/iPhoneOS11.4.sdk/System/Library/Frameworks \
          -L/tmp/iPhoneOS11.4.sdk/usr/lib
    
    - name: Build arm64 (64-bit)
      run: |
        clang \
          -arch arm64 \
          -miphoneos-version-min=7.0 \
          -dynamiclib \
          -o gdps_proxy_arm64.dylib \
          gdps_proxy.c \
          -framework Security \
          -framework CoreFoundation \
          -framework CFNetwork \
          -isysroot /tmp/iPhoneOS11.4.sdk \
          -F/tmp/iPhoneOS11.4.sdk/System/Library/Frameworks \
          -L/tmp/iPhoneOS11.4.sdk/usr/lib
    
    - name: Create FAT binary
      run: |
        lipo -create \
          gdps_proxy_armv7.dylib \
          gdps_proxy_arm64.dylib \
          -output gdps_proxy.dylib
        
        # Verify it's a FAT binary
        lipo -info gdps_proxy.dylib
        file gdps_proxy.dylib
    
    - name: Code sign (optional, for testing)
      run: |
        codesign -f -s - gdps_proxy.dylib || true
    
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: gdps-proxy-dylib
        path: |
          gdps_proxy.dylib
          gdps_proxy_armv7.dylib
          gdps_proxy_arm64.dylib
